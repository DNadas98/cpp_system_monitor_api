# Copy and rename to .env, modify values as needed

APP_PORT=8080

DB_HOST=crow_db # localhost for running without docker-compose
DB_PORT=5432
DB_EXPOSED_PORT=54321
DB_USER=devuser
DB_PASSWORD=devpw
DB_NAME=crowapidb
